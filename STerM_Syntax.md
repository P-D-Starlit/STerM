# STerM Syntax

- STerM 문법과 Python의 관계
  - STerM의 표준 문법은 Python 표준 규격에 준해 사용합니다. 다만, `in`, `not`, `and` 등의 명시적 연산자를 사용하지 않고 C언어의 연산자 `!`, `&&` 등을 표준 규격으로 정의합니다.
  - STerM의 표준 하이라이팅은 Python과 동일하게 설정할 수 있으나, Python 규격의 문법과 엄연히 다릅니다.
  - STerM은 Python과 다르게 Interpreter 언어가 아닌 Virtual Machine 언어이므로 Python과 동일하게 취급할 수 없습니다.
  - Python을 충실히 학습하셨다면 무리 없이 STerM을 접하실 수 있도록 설계되었습니다. STerM 학습하시기 전에 Python을 선행학습하신다면 도움이 많이 됩니다.(Class 전까지만 학습하셔도 좋습니다.)

## 1. STerM 표준 문법 규격 - 기본

### 1-1. 문장의 구분

- STerM의 문장 구분은 줄바꿈으로 함을 원칙으로 한다. 서로 다른 구문은 줄을 바꿈으로써 구분할 수 있다.
  ```Py
  CONSOLE << f"Index = {i}, Value = {val}" << CONSOLE.ln
  WaitKey
  ```
  - 위 구문의 경우 CONSOLE로 시작하는 구문은 CONSOLE.ln까지가 하나의 문장이다.
  - WaitKey는 그 자체로 다른 문장이다.

- 단, Time Delay가 존재하는 구문은 같은 줄에 표시할 수 있다. 이때, 구분자는 `;`를 사용한다.
  ```Py
  CONSOLE << f"Index = {i}, Value = {val}" << CONSOLE.ln; Delay 10
  ```
  - 이 구문에서 Delay 10은 앞의 구문과 다른 문장으로 취급하나, 간결성을 높이기 위해 한 줄에 작성하였다.

- 문장의 구분은 한 줄에 여러 코드를 작성해도 좋으나, 가능하면 메인 코드는 하나의 줄에 1개만 넣고, Delay와 같은 부속 코드만 메인 코드 앞뒤로 붙이는 것을 권장한다.
- 문장의 끝에는 원칙적으로 세미콜론을 붙이지 않는다. 단, 세미콜론을 붙인다고 해서 컴파일 오류 요건이 될 수 없다.
- 문장의 구분이 필요한 경우 세미콜론을 반드시 붙여야 한다.

### 1-2. 괄호의 사용

- 함수의 인수로 값을 넣을 경우 괄호를 반드시 사용한다.
- 인수가 여러 개인 경우 `,`로 구분한다.
  ```Py
  PrintValue(arr, sizeof(arr) / sizeof(int))
  ```
- Delay 구문은 괄호를 사용하지 않는다.
  ```Py
  Delay 1023
  ```
- 조건문, 반복문의 인수는 괄호를 사용하지 않는 것을 원칙으로 한다. 단, 괄호를 사용한다고 해서 컴파일 오류를 발생하지 아니한다.
  ```Py
  if a == 3:
      CONSOLE << "a == 3" << CONSOLE.ln
      while a > 0:
          CONSOLE << f"a == {a}" << CONSOLE.ln
          Delay 1023
          a -= 1
  ```
### 1-3. 연산자
- STerM에서는 int, float형은 물론 3대 객체에서도 연산자가 제공된다.
- STerM의 연산자 우선순위는 C언어에 준한다. 단, 비교 연산자는 모두 우선순위가 동등하다.
- STerM은 비교 연산자를 연쇄 사용 시 연쇄 비교 연산이 가능하다.
  ```Py
  if -10 <= a < 10:
      ..
  ```
- STerM에서 +=, -= 등의 연산자는 연산 후 대입을 의미한다.
  - a += 1의 경우 a = a + 1과 동등하게 처리한다.
- STerM에서는 전위/후위 연산자를 지원한다.
  - a++, ++a 모두 사용 가능하다.
  - 2회 이상 전/후위 연산도 지원된다. ++a-- 등과 같은 형태로 쓸 수 있다. ~~사실상 a+1과 동등해 의미 없다.~~
### 1-4. 리터럴
- 숫자 리터럴
  - 정수형 리터럴은 숫자로만 정의하거나 16진수의 경우 앞에 0x 접두어를, 2진수는 0b 접두어를, 8진수는 0o 접두어를 붙인다.
  - 소수형 리터럴은 소수점을 포함한 숫자로 정의한다. 이때, 정수의 경우 1.0과 같이 소수점 아래의 값은 0으로 정의하면 된다.
- 문자열 리터럴
  - 단순 문자 리터럴은 정수형 리터럴로 인식되며, 작은따옴표(`''`) 안에서 작성한다.
  - 고정 문자열 리터럴은 ref형으로 인식되며, 큰따옴표(`""`) 안에서 작성한다.
    - 단, 배열로 정의하는 경우 ref형이 아닌 str형으로 강제 형변환된다. 배열의 컨셉 자체가 값을 바꿀 수 있음을 전제로 하기 때문이다.
  - 유동 문자열 리터럴은 str형으로 인식되며, f-string(`f""`)으로 정의함을 원칙으로 한다.
    - 유동 문자열은 종료 문자를 포함하여 256바이트까지 사용할 수 있다.
### 1-5. 변수의 정의
- STerM에서 변수의 정의는 Python 방식에 준하여 사용하되, Python은 동적 타입이라면 STerM에서는 정적 타입으로, 한 번 선언된 변수의 자료형은 변수가 소멸할 때까지 바뀌지 않는다.
- 변수 선언 : 함수의 인수로 변수를 넣어야 하나 값은 필요하지 않을 경우 초기화 없이 아래와 같이 선언할 수 있다.
  ```Py
  a = int
  ```
- 변수 초기화 : 선언 유무에 상관 없이 초기화만으로 변수를 정의할 수 있다. 다만, 초기화 시 리터럴의 자료형이 적용되며, 소멸될 때까지 자료형이 변하지 아니한다.
  ```Py
  a = 0 # a는 정수 0으로 초기화하면서 선언되었으므로 소멸 시까지 정수 자료형 유지.
  a = 3.14 # a는 소수로 초기화되었으므로 float형 자료형이 된다.
  ```
  - 변수 기본 자료형 리터럴은 int형과 float형뿐이다.
  - 소수 뒤에 L이나 D를 붙이면 double형 사용 가능하다.
  - STerM에서는 int, float, double형 3가지로 대부분 코드를 실행할 수 있으며, 16진수 처리가 필요한 경우 unsigned int가 필요하므로 4자리씩 끊어서 해석할 것을 권장한다.
- 자료형 선언 및 초기화가 동시에 이루어지는 경우
  ```Py
  a = float(0)
  ```
  - 실질적으로 강제 형변환 함수가 작동하여 값을 선언하는 것이므로 초기화되는 값은 정수지만 소수로 형변환된 상태로 0.0이 초기화되면서 변수가 선언된다.
- 배열 선언 시
  - 배열 선언은 크기를 정의하는 경우 선언만 하는 것을 권장하며, 값으로 초기화하면 크기를 변경할 수 없으니 유의해야 한다.
  - 크기를 우선 정의한 후 앞부분 데이터를 저장하는 것을 권장한다.
    ```Py
    a = int[20]
    a = [1, 0, 2, 3]
    ```
  - 처음부터 값으로 초기화하면 초기화할 당시의 크기로 고정된다.
    ```Py
    a = [1, 0, 2, 3] # 배열 크기는 4칸이 됨.
    ```
  - 값으로 초기화하는 경우 배열의 자료형은 첫 번째 인수의 자료형으로 설정되며, 나머지 인수는 첫번째 자료형으로 강제 형변환 된다.
  - 배열 안에 일반 문자열을 넣는 경우 일반 문자열이 ref형 리터럴임에도 str형으로 초기화된다.
### 1-6. 포인터와 레퍼런스
- STerM에서는 공식적으로 포인터 기능을 제공하지 않는다. 다만, 포인터와 비슷한 용도로 사용할 수 있는 레퍼런스를 지원한다.
- 변수의 레퍼런스
  - 변수의 레퍼런스는 `_a`와 같이 사용할 수 있다.
  - 특정 객체의 특정 속성값이나 배열의 레퍼런스를 구하는 경우 `_()` 연산자를 쓰면 구할 수 있다. 또, 특정 변수가 레퍼런스 변수인지 아니면 일반변수인지 구분이 안되는 경우도 `_()` 연산자 안에 넣으면 된다.
- 레퍼런스 변수를 사용하기 위해서는 `a = _int`와 같이 변수 정의할 때 자료형 앞에 `_`를 붙여서 정의한다.
  - 이때, a의 레퍼런스값은 `a_`에서 확인할 수 있다.
  - 일반 변수의 레퍼런스 값은 `_b`와 같은 형태로 구분해서 정의한다.
  - 일반 변수 b의 레퍼런스를 공유하는 경우 `a_ = _b`로 작성하며, 이때 `a = 20`과 같은 대입 연산을 취할 경우 변수 b의 값이 바뀐다.

## 2. STerM 표준 문법 규격 - 객체 사용법

### 2-1. CONSOLE 객체

#### 2-1-1. CONSOLE 객체의 정의

- STerM의 `CONSOLE` 객체는 `CONSOLE` 단독으로 정의하거나, 여러개의 콘솔 창이 사용되어야 하는 경우 `CONSOLE[n]`의 형태로 정의하며, CONSOLE 객체는 배열의 형태로 정의는 가능하나, 대입 연산 등을 사용하여 2개 이상의 콘솔 객체를 정의하지 않음을 원칙으로 한다.

#### 2-1-2. CONSOLE 객체의 생성 및 소멸

- CONSOLE 객체의 생성과 소멸은 Windows 운영체제에서 수행한다.
- STerM 문법에서는 CONSOLE 객체의 생성 및 소멸에 관한 코드는 정의하지 않는다.
- STerM 프로그램 시작 시점에서 CONSOLE 객체는 운영체제에서 자동 생성되며, 프로그램 종료 또는 운영체제에 심각한 오류를 끼치는 문제가 생길 시 즉시 소멸한다.
- CONSOLE 객체가 소멸하면 프로그램은 반드시 종료된다.
  - CONSOLE 객체 중 최상위 CONSOLE은 CONSOLE[0]과 동일한데, 이게 종료되면 하위 객체는 전부 종료되며, 프로그램은 종료되어야 한다.

#### 2-1-3. CONSOLE 객체의 연산

- CONSOLE 객체는 `<<` 연산자를 이용하여 입출력을 수행한다.
  - `<<` 또는 `>>` 연산자 방향으로 CONSOLE이 있는 경우 CONSOLE 창에 출력한다.
  - `<<` 또는 `>>` 연산자 방향으로 변수가 있는 경우 변수에 연산 결과를 출력한다.
    - 이때, 변수의 자료형은 int, float, str형이 유효하다.

#### 2-1-4. 시스템 기능

- 아래 3가지 기능은 괄호 없이 사용한다.
  - Delay XXX : 시스템을 잠시 멈추고 계속 실행한다는 의미이지만, 원래 Sleep과 동일 규격으로 시스템을 잠시 멈추었지만 COM포트에서 들어오느데이터를 씹어버리는 문제가 있어 Sleep과 분리하였다.
  - Sleep XXX : 시스템을 잠시 멈추는 의미. 이쪽은 Delay와 다르게 Sleep모드 진입을 의미하여 COM포트에 들어오는 데이터를 씹는 문제가 있을 수 있다. 다만, 이쪽은 사용 시 CPU 소모량이 줄어들어 과부하 상태를 방지할 수 있다.
  - WaitKey : 키보드의 아무거나 입력할 때까지 대기한다.
- 아래 기능은 함수와 동등하게 사용한다.
  - getch() : 키보드 입력 시까지 대기 -> 입력 시 입력된 값을 반환하는 함수.
  - kbhit() : 키보드 입력 시까지 대기 없이 입력이 감지되면 0이 아닌 값을 반환한다.
  - 2개의 함수를 같이 사용하여 특정 키보드 입력을 감지할 수 있다.

#### 2-1-5. 비표준 함수

- 아래 기능은 비표준 함수의 일종으로 InstantTTY상에서 최적화된 코드 사용에만 쓰인다.
  - Print1 : 글자 색 입히지 않고 원문 그대로 출력하는 함수.

### 2-2. COM객체

#### 2-2-1. COM 객체의 정의
- COM 객체의 졍의는 STerM 내부 알고리즘에 의해 자동으로 진행한다.
  - COM포트 정의를 위해 레퍼런스 배열을 정의해야 한다.
  - 레퍼런스 배열은 아래와 같이 정의하면 자동으로 COM객체가 정의된다. 이때, 전역변수로 정의한다.
    ```Py
    COMPORT[i] = ["COM3", "COM4", ...]
    ```
- COM 객체는 COM[x].Open()을 통해 연결할 수 있다. 이때, 연결 실패 시 0을, 성공 시 1을 반환한다.
- COM 객체는 COM[x].Close()를 이용하여 객체를 소멸할 수 있다.
- COM 객체의 기본 설정 값은 115200baud / 8bit / No Parity / 1 Stop Bit이다.
  - 이중 Baud Rate는 바뀌는 경우가 잦은데, 1500000baud로 사용하고 싶다면 COM[x].Open(1500000)으로 열자.
- 자동화 검증 테스트 중 접촉불량으로 COM포트가 끊어질 경우 다시 연결해야 하므로 중간에 끊었다 다시 연결하는 기능을 추가하면 좋다.

#### 2-2-2. COM객체의 Method
- COM 객체는 Method를 사용하여 처리함을 원칙으로 한다.
- `COM[x].Open()`
  - COM포트를 열 때 사용한다.
  - 인수 : baud rate(선택) : 보드레이트를 기본값(115200)이 아닌 값으로 사용할 때 적용.
- `COM[x].Close()`
  - COM포트를 닫을 때 사용한다.
  - 이 연산을 한다고 완전히 객체가 소멸되지 않고, 재사용 가능하나, 다른 시스템과 교차 사용하는 경우 반드시 이 함수를 사용한다.(이 함수 미사용시 PuTTY나 Tera Term이 해당 COM포트를 못잡는 이슈가 있음.)
- `COM[x].Available()`
  - COM포트에서 읽을 수 있는 바이트 수를 구한다.
- `COM[x].Read()`
  - COM포트에서 1바이트를 읽는다.
  - 인수에 문자열 객체를 넣을 수 있는데, 이 경우 문자열 객체에 최대 255바이트 읽은 값을 반환한다.
    - 이렇게 사용하는 경우 C언어 레벨에서 빠르게 처리하기 때문에 TTY 기능을 빠르게 구현할 수 있다.
- `COM[x].Peek()`
  - COM포트에서 바로 다음으로 읽어들일 값을 반환한다.
  - 문자열을 넣을 경우 최대 255바이트까지 반환한다.
- `COM[x].Flush()`
  - COM포트에 저장된 내용을 비운다.
  - 이 함수를 사용하는 경우 버퍼 오버플로우를 줄일 수 있다.
- `COM[x].Wait()`
  - 시리얼 포트에 값이 들어올 때까지 Timeout만큼 대기
  - Timeout은 인수로 직접 넣을 수 있으며, 넣지 않은 경우 기본 값으로 실행된다.
- `COM[x].Scan(문자열)`
  - 자동화 테스트에서 가장 강력한 기능 중 하나로, 줄바꿈이 감지되면 양수를, 감지되지 않으면 음수를 출력하여 양수가 출력될 때만 감지하여 문자열을 확인해 비교한다.
  - 현재 최적화가 더욱 필요한 부분으로, 계속해서 사용성을 높일 계획이다.

#### 2-2-3. COM 객체의 연산

- COM 객체는 문자열과의 `<<`, `>>` 연산이 가능하다.
- COM 객체와의 연산에서는 손실 방지, 안전성을 위해 `<<`, `>>` 연산자로 문자열이 아닌 변수에 받아오는 것은 허용되지 않는다. 즉, 반드시 문자열을 거쳐서 숫자로 변경해야 한다.
  - 문자열을 숫자로 바꾸는 것은 문자열 가공 내용 참고.

#### 2-2-4. 비표준 문법

- 아래 기능은 비표준 문법으로 InstantTTY상에서 최적화된 코드 사용에만 쓰인다.
  - *COM[x] : COM[x].Available()와 동일 기능.

### 2-3. 파일 객체

#### 2-3-1. 파일 객체의 생성

- FILE_Setup : 파일 쓰기 전용, Open/Close 없이 누적해서 파일에 쓸 때 사용한다.
  - 장점 : TTY상의 로그 저장할 때 유리하며, Open/Close 기능을 사용할 필요가 없다.
  - 단점 : 읽기 자체가 지원이 되지 않는다.
  - 사용법
    ```Py
    myFile = FILE_Setup("myFile.txt", "at", "\n")
    ```
  - 모드 및 줄바꿈
    - 줄바꿈을 포함한 문자열이 들어올 때의 처리 방식에 따라 모드는 `"at"` 또는 `"ab"`로 선택한다.
      - `\r\n`이 통째로 들어오게 설계된 AP/MCU에서는 ab를 쓰는 게 좋다.
      - `\n`만 들어오게 설계된 AP/MCU에서는 at를 쓰는 게 좋다.
    - 줄바꿈 문자 설정은 `"at"`에서는 `"\n"`을, `"ab"`에서는 window 운영체제상에서는 `"\r\n"`으로 설정한다.

- FILE_Open : 파일 읽기에 주력으로 사용되는 규격. 쓰기도 가능하다.
  - 장점 : 파일 읽기가 가능하다.
  - 단점 : Open/Close를 직접 써야 한다.
  - 사용법 : 생성 문법은 아래와 같이 작성하면 된다.
    ```Py
    myFile = FILE_Open("myFile.txt", "rt", "\n")
    ...
    myFile.Close()
    ```
  - 이 방식으로 파일을 연 경우 반드시 myFile.Close()로 닫아야 오류가 발생하지 않는다.
  - 파일을 열다가 중간에 Close한 경우 오류가 발생할 수 있다.(특히 읽기모드로 한경우 손상은 없지만 쓰기모드로 연 경우 손상이 발생할 수 있으니 주의!)

- 모드
  - "wt" : 쓰기 모드, 파일을 항상 새로 만들어 덮어쓴다.
  - "rt" : 읽기 모드
  - "at" : 누적 쓰기 모드, 파일이 없으면 새로 만들어 쓰고, 있으면 추가한다.
  - "wb" : 바이너리 쓰기 모드, 파일 자체를 바이너리로 생성하여 쓴다.
  - "rb" : 바이너리 읽기 모드
  - "ab" : 바이너리 누적 모드
  - 바이너리 모드와 텍스트 모드는 거의 차이가 없으나 줄바꿈 문자 처리에서 큰 차이를 보인다.
    - STerM에서는 줄바꿈 처리까지 정확하게 정의하기 위해 줄바꿈 문자도 인수로 넣도록 하였다.

#### 2-3-2. 쓰기 파일에서의 처리 방법

- 적용 범위 : FILE_Setup으로 열었거나, FILE_Open으로 쓰기 모드로 연 경우.
- `(file).ln` : 줄바꿈 문자열을 의미한다.
- `(file) <<` : 파일 객체에 문자열을 쓴다.
  - 원칙상 문자열만 사용 가능하며, 값을 넣기 위해서는 f-string을 사용하면 된다.
  - 출력 끝에 줄바꿈을 넣기 위해 아래와 같이 작성할 수 있다.
    - `(file) << "string" << (file).ln`

## 3. STerM 표준 문법 규격 - f-string 출력

### 3-1. f-string이란?

- f-string은 Python의 문자열 포매팅 방식으로, 보다 직관적으로 문자를 출력할 수 있게 만들어진 포매팅 방식이다.
- Starlit 언어에서는 %d 등을 사용한 포매팅을 자주 사용했지만, STerM은 f-string만 사용하는 쪽을 권장한다.
- 변수를 넣지 않더리도 f-string을 사용하면 그 자체로 문자열 객체로 인식하기 때문에 문자열 객체와 레퍼런스 구별이 필요할 때 사용할 수 있다.
  - ref -> string형으로의 변환은 유연하게 작동하나, string형을 ref형으로 받는거는 기술적으로 정의가 되어 있지 않다.

### 3-2. f-string의 사용 - 정수형

#### 3-2-1. 기본 포맷 : 10진수, 가변 자리수

```Py
main:
    a = 0
    while 1:
        CONSOLE << f"/0a = {a}"
        ch = getch()
        when ch:
            case 1: a -= 1
            case 2: a += 1
            case 0: break
```
- 위와 같은 코드가 있다고 할 때, 처음 출력은 `a = 0`이 나오고, 1 또는 2를 입력하면 값이 바뀌어 출력된다.
- 가변 자리수이기 때문에 `10`이 출력되고 다시 a값을 1 감소했을 때 `9`를 출력하지만, `10`은 2자리고 `9`는 1자리이므로 덮어쓰도록 설계가 된 경우 `90`으로 보이게 된다. 따라서 1자리 수를 고정 출력하거나 CSV 파일을 만들 때만 기본형을 사용한다.

#### 3-2-2. 자리수를 맞추는 경우

```Py
CONSOLE << f"a = {a:3d}" << CONSOLE.ln
```

- 자리수를 3자리로 맞추는 경우 변수 뒤에 `:3d`를 붙인다. 이때, 부호를 포함해서(음수인 경우) 3자리의 10진수를 출력하며, 3자리 미만의 숫자인 경우 맨 앞에 공백을 출력한다.
- 주로 값을 조절하거나 동일한 위치에 실시간으로 값을 확인할 때 이 방식을 사용할 수 있다.

#### 3-2-3. 앞자리 수에 0을 출력하는 경우

-  0012, 0020과 같이 특정 자리수를 맞춰서 출력하는 경우 `:04d`를 사용하면 된다. 이때, 앞자리 수에 공백으로 채웠던 것을 0으로 채운다.

#### 3-2-4. 2진수, 8진수, 16진수 출력해야 하는 경우

- 다음 예제와 같이 출력하면 된다. 일반적으로 2진수, 8진수, 16진수 사용 목적에 맞게 앞자리 수는 0으로 채우는 경우가 일반적이다.
- 이진수 8자리(1바이트) : `{a:08b}`
- 8진수 4자리(6바이트) : `{a:04o}`
- 16진수 4자리(2바이트), a~f는 소문자 : `{a:04x}`
- 16진수 8자리(4바이트), A~F는 대문자 : `{a:08X}`

### 3-3. f-string의 사용 - 소수형

- 소수는 정수와 다르게 10진수 고정으로 사용한다.
- STerM에서는 공학용 단위표시를 사용할 수 있다.
  - STerM 자체가 회로 디버깅용 프로그램 컨셉으로 제작되어 단위표시를 지원한다.

#### 3-3-1. 소수 포매팅

- 기본형 : 포매팅 내용을 따로 정의하지 않는 경우 소수점 아래 2자리까지만 출력한다.
- 자리수 맞추지 않는 경우 : 보통은 가독성을 위해 소수점 아래 자리수 표시 정도는 한다.
  - 소수점 아래 3자리 표시 : `{a:.3f}`

#### 3-3-2. 자리수 맞추는 경우

- 콘솔 창에서 그 위치에 출력하는 경우 자릿수를 맞춰야 한다.
- 정수 3자리 + 소수점 아래 3자리 : 소수점도 1자리로 포함하므로 총 7자리를 차지
  - `{a:7.3f}`
- 앞자리를 공백이 아닌 0으로 채우는 경우 : `{a:07.3f}`

#### 3-3-3. 공학용 SI 단위표시

- STerM의 소수형은 C/C++의 float 또는 double에 해당하는 부동소수점형으로 표시할 수 있는 범위가 매우 넓은 특징이 있어 SI 단위표시를 지원한다.
- SI 단위표시 리터럴 : `a = 1.0m`이라고 작성하면 1.0밀리를 의미하며, 밀리에 해당하는 0.001이 곱해져서 0.001이 저장된다.
- SI 단위표시 출력 : 정수 범위가 1.0 이상이 되도록 조정하여 SI 단위표시에 맞게 출력된다. 1.0 이상이 되도록 맞추기 때문에 0.5m의 경우 500mm로 출력된다.
  - 이때, 단위 표시는 측정의 measure에서 딴 m을 사용해 `{a:.3m}`과 같이 표시한다.
  - CSV 파일을 만들 때는 이렇게 적으면 계산이 불가능하니 유의해야 한다.
- ~~SI 단위 입력(추후 지원 예정) : 문자열에서 숫자로 시작하는 경우 SI단위까지 읽는 함수도 추후 지원 예정이다.~~

### 3-4. 그 외 사용

#### 3-4-1. 문자열 객체

- 문자열 객체 또는 레퍼런스형을 넣으면 문자열을 그대로 출력한다.
- 32비트 초과하는 데이터는 문자열 형식으로 처리하는 것이 일반적인데, 그대로 출력하려면 문자열 객체로 출력하면 된다.


## 4. 주요 구문과 Python과의 차이점

- 구문 자체는 Python과 비슷한 점이 많아서 Python과의 차이점 중심으로 서술한다.

### 4-1. 조건문 `if, elif, else`

- STerM에서는 Python과 동일한 if문을 제공한다.
- `if` 뒤에는 괄호를 쓰지 않고 조건을 적는다.
- 단항 연산은 피연산자와 붙여 적고, 2항 이상의 연산자는 연산자 앞뒤로 띄어 적는다. 단, 연산자 앞뒤로 모두 붙여 적는 것도 허용하나, 단항 연산과의 혼동을 피하기 위해 한쪽만 붙이는 것은 금지한다.
- `if`문을 우선 실행하고, `if`문이 거짓으로 판단되면 `elif`나 `else`를 실행한다.
- `else`는 인수를 받지 않는 가장 나중에 작성하는 조건문이다.
- `elif`는 앞의 `if`나 `elif` 또는 `for`, `while`문 뒤에 적으며, 앞의 구문이 거짓이고, `elif`문의 내용이 참일 때 실행한다. 따라서 `elif`로 시작하는 구문도 금지한다.
- `elif` 대신 `else if`로 적는 것도 경로의존성에 의해 가능은 하나, 권장하지 않는다.
- `if`, `elif`, `else`문은 코드 간결성 보장을 위해 간단한 코드는 한 줄에, 복잡한 코드는 그 다음 줄에 작성한다. 단, 줄 바꾸는 경우 들여쓰기를 반드시 실행해야 한다.

- Example 1은 조건문 작성 후 줄바꿈 후 작성하는 예제이다.
  ```Py
  # Example 1
  main:
      a = 0
      CONSOLE << "Input A: " >> a << CONSOLE.ln
      if a == 0:
          CONSOLE << "A is Zero." << CONSOLE.ln
      elif a > 0:
          CONSOLE << "A is Positive." << CONSOLE.ln
      else:
          CONSOLE << "A is Negative" << CONSOLE.ln
  ``` 

- Example 2은 조건문 작성 후 줄바꿈 없이 작성하는 예제이다.

  ```Py
  # Example 2
  main:
      a = 0
      CONSOLE << "Input A: " >> a << CONSOLE.ln
      if a == 0: CONSOLE << "A is Zero." << CONSOLE.ln
      elif a > 0: CONSOLE << "A is Positive." << CONSOLE.ln
      else: CONSOLE << "A is Negative" << CONSOLE.ln
  ```

- if문 내부 논리 연산자는 C/C++ 방식을 따른다.
  - `if a == 0 or a == 2:` (x)
  - `if a == 0 || a == 2:` (o)

### 4-2. 조건문 when

- when문은 C/C++의 switch문을 살짝 변형한 것이다.
- when에 변수를 넣고 case문을 넣으면 다음 case 전까지 실행된다.
- Notepad++에 언어를 Python으로 설정해놓고 사용하는 경우 when문은 하이라이팅되지 않는다. Python에서는 해당 구문을 지원하지 않기 때문이다. 오히려 C/C++의 switch문과 비슷하지만 작동 방식의 차이가 있어 when문으로 사용한다.

#### 4-2-1. 레지스터 값 읽기

- GPIO 레지스터 중 MODER의 값을 Serial 통신을 통해 읽었다고 가정하면, when문을 이용하여 모드를 확인할 수 있다.
  ```Py
  # 변수 moder에 2비트 데이터를 저장했다고 가정합니다.
  when moder:
      case 0: CONSOLE << f"MODE : INPUT" << CONSOLE.ln
      case 1: CONSOLE << f"MODE : OUTPUT" << CONSOLE.ln
      case 2: CONSOLE << f"MODE : Alternate Function" << CONSOLE.ln
      case 3: CONSOLE << f"MODE : Analog" << CONSOLE.ln
  ```
- 만약에 "정의되지 않은 값"을 Reserved라 출력하고자 한다면 아래와 같이 써도 좋다.
  ```Py
  when reg:
      case 0: CONSOLE << f"MODE : INPUT" << CONSOLE.ln
      case 1: CONSOLE << f"MODE : OUTPUT" << CONSOLE.ln
      case 2: CONSOLE << f"MODE : INPUT_PULLUP" << CONSOLE.ln
      default: CONSOLE << f"MODE : RESERVED" << CONSOLE.ln
  ```

#### 4-2-2. 범위 출력

- 특정 범위를 출력하고자 한다면 아래와 같이 출력하자.
  - `>10` : 10보다 클 때 실행
  - `>=10` : 10보다 크거나 같을 때 실행
  - `<10` : 10보다 작을 때 실행
  - `<=10` : 10보다 작거나 같을 때 실행
  - `1:10` : 1부터 10까지일 때 실행

#### 4-2-3. C/C++와의 차이

- 변수를 넣어도 되는가?
  - C/C++ : case 뒤의 숫자는 변수를 넣을 수 없다. 간혹 번수를 넣은 것처럼 보인다면 최적화 과정을 통해 상수로 처리되거나 아예 매크로에 의해 정의된 상수이다.
  - STerM : case 뒤에는 변수를 넣을 수 있다. 만약 변수를 넣는다면 그 변수의 값 기준으로 판별한다.
    - 예1: 뒤에 `case a`, `case 1`이 있고 a가 1이라면 a쪽을 먼저 실행하고 `case 1`쪽은 아예 실행이 되지 않는다.
    - 예2: `>a`, `<a`와 같은 형태는 변수 기준으로 비교한다.

- if문을 넣으면 어떻게 되는가?
  ```Py
  main:
      for i:-20:20:
          when i:
              if -5<=i<=5: CONSOLE << f"/si={i}" << CONSOLE.ln
              case -10:<10: CONSOLE << f"/ri={i}" << CONSOLE.ln
              default: CONSOLE << f"/gi={i}" << CONSOLE.ln
      WaitKey
  ```
  ```Py
  main:
      for i:-20:20:
          if -5<=i<=5: CONSOLE << f"/si={i}" << CONSOLE.ln
          when i:
              case -10:<10: CONSOLE << f"/ri={i}" << CONSOLE.ln
              default: CONSOLE << f"/gi={i}" << CONSOLE.ln
      WaitKey
  ```
  - 두 실행 결과는 완벽히 같다. 즉, case와 if를 동시에 쓰는 것은 별 의미가 없다.
  - 오히려 case와 default에 if를 넣는 것은 좋지 않은 코드인데, case문 구조를 훼손하여 작동이 이상해질 것이다.
  - C/C++언어에서는 switch문 case와 break 사이 밖에 넣은 if문은 실행 자체가 되지 않는다.
- 2가지 이상의 공통 case의 경우?
  - C/C++에서는 case A: 직후에 case B:를 써서 2가지 이상의 공통 case로 사용 가능하며, break 없이 이어서 실행하는 것도 허용된다.
  - STerM에서는 case A: 직후에 case B:를 쓴다면 A에 대한 실행이 되지 않는다. 따라서 case A,B:와 같이 사용한다. 만약 이런 방식으로 쓰고자 한다면 when문 대신 switch문을 직접 갖다 써도 좋다.

### 4-3. 반복문 `while`

- 조건이 참이면 그 구문을 반복하는 단순 반복문.
- while 뒤에 1이 있으면 무한 반복 실행한다. 단, 내부에 break가 있으면 무한 반복을 빠져나갈 수 있다.
- STerM에서는 `goto`문이 따로 존재하지 않으므로 2회 이상 중첩된 반복문을 빠져나갈 떄는 `break 3`과 같이 사용한다. break 뒤에 들어가는 인수는 변수를 넣을 수 없으며, 들여쓰기 기준으로 들여쓰기 횟수 차이만큼 빠져나간다. 따라서 `break 2`는 일반적으로 반복문 1회 탈출이라 잘 사용하지 않고, `break 3` 이상을 사용한다.
- C/C++와 같이 `continue`라는 구문이 있으며, 이것도 break문과 동일하게 뒤에 인수를 넣으면 인수 기준의 반복문을 다시 반복한다.
  - `continue`는 설계상 특정 구문 재실행을 의미하여 반복과 관련 없는 if문쪽과 인수로 연결한다면 if문을 다시 실행하게 되며, 사실상 이런 구조는 `do...while`문 대용으로 사용 가능하다.
- while문 뒤에 들여쓰기 없이 써도 된다. 이 경우 조건이 참으로 바뀔 때까지 계속 대기한다.
  - 사실상 wait나 delay같은 성격이 있다.
  - 일반적으로 레지스터 플래그 값이 바뀔때까지 기다릴 때 사용하는 구문이다.
- while문 예제
  - 시리얼 통신으로 줄바꿈이 들어올 때까지 대기하기
    ```Py
    while COM[1].Scan(st) <= 0
    ```
  - 버튼 누를 때까지 대기
    ```Py
    while !kbhit() # 버튼 누를 때까지 대기
    key = getch() # 어떤 버튼 입력되었는지 받기(화살표키 누를 시 오작동할 수 있음.)
    ```
  - 무한 반복 실행
    ```Py
    while 1:
        CONSOLE << f"/0Data = {data}" << CONSOLE.ln
        if kbhit():
            while getch() # 입력한 값 버리기
            break # 무한반복문 탈출(들여쓰기 없이 작성.)
    ```

### 4-4. 반복문 for

#### 4-4-1. 유한 반복

- 순차 반복문으로 유한한 횟수만큼 반복할 때 주로 사용한다.
- 작성 방법 : `for i:(시작):(끝)` => 시작부터 끝까지 1씩 증가하며 반복.
  - 이때 최종적으로 i는 (끝)+1이 저장된다. 단, 역방향 반복인 경우 (끝)-1이 저장된다. 하지만 이렇게 저장되는 i값을 제대로 쓰는 경우는 거의 없다.
- 예제 : 1부터 10까지 출력하기
  ```Py
  for i:1:10:
      CONSOLE << "{i}"
  ```

#### 4-4-2. 유한 반복 (2)

#### 4-4-3. 인덱스 증가 반복

- 문자열을 순차 검사하여 반복하는 것도 가능하다. 이 때는 아래와 같이 쓴다.
- `for (시작), (조건), (증감):`







