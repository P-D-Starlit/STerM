# STerM 사용 시 주의사항

## 목차




## 1. `<<` 연산자 남발은 컴파일 속도 저하의 원인이 될 수 있습니다.

- 문자열에 값을 출력하는 방법은 아래와 같이 2가지로 나눠 볼 수 있습니다.

    ```Py
    CONSOLE << "a + b = " << a+b << CONSOLE.ln
    ```

    ```Py
    CONSOLE << f"a + b = {a+b}" << CONSOLE.ln
    ```

- 이 때, 첫번째 방법은 좋은 방법이 아닙니다. CONSOLE 객체를 제외한 다른 객체는 문자열(또는 레퍼런스)을 제외한 다른 자료형을 `<<` 연산자를 지원하지 않습니다(이쪽은 Warning이 아닌 Error로 간주).
- Starlit 컴파일러는 특성상 `<<` 연산자를 5회 이상 사용할 경우 컴파일러의 속도가 현저하게 느려지는 약점이 있습니다. 사용 시 주의 부탁드립니다.

## 2. 약식 연산자 사용은 권장하지 않습니다.

- 일부 객체는 약식 연산자가 적용된 경우가 있는데, 가능하면 사용하지 않는 것을 권장합니다.






